<!DOCTYPE html>
<html lang="en">
<head>
    {{>header}}
</head>
<body>
    <h1>{{title}}</h1>
    <div class="text-right">
                <a href="/createnewentries" class="btn btn-primary">
                    Create a new Goal
                </a>
            <div class="text-right" style="margin-bottom: 1em;">
                    {{^user}}
                <a id="login" href="/login" class="btn btn-primary">
                        Sign in
                    </a>
                <a id="login" href="/register" class="btn btn-primary">
                    Register
                </a>
                    {{/user}}
                    {{#user}}
                <a id="logout" href="/logout" class="btn btn-primary">
                    Sign out
                </a>
                    {{/user}
            </div>
                <div class="text-right" style="margin-bottom: 1em;">   
                    {{^user}}
                <a id="login" href="/register" class="btn btn-primary">
                    Register
                </a>
                <a id="login" href="/login" class="btn btn-primary">
                    Sign in
                </a>
                    {{/user}}
                    {{#user}}
                <a id="logout" href="/logout" class="btn btn-primary">
                    Sign out
                </a>
            {{/user}}
        </div>
        {{#entries}}
            <div>
                {{date}}
            </div>
            <div>
                {{goal}}
            </div>
            <div>
                {{#goalBreakdown}}
                {{goal1}}<br>
                {{goal2}}<br>
                {{goal3}}<br>
                {{/goalBreakdown}}
            </div>
            <div>
                {{#achievements}}
                {{ach1}}<br>
                {{ach2}}<br>
                {{ach3}}<br>
                {{/achievements}}
            </div>
            <div class="card-text">
                Written by <a href="/posts/?user={{user}}">{{user}}</a>, on
                {{date}}
            </div>
            <div class="text-right">
                <a href="/update" class="btn btn-primary">
                    Update
                </a>
                <a href="/delete" class="btn btn-primary">
                    Delete
                </a>
            </div>
            <hr> 
        {{/entries}}
        {{^entries}}
        No entries yet.
        {{/entries}}    
   </body>
</html> 